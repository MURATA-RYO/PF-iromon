<% if @range == '2' %>
<h1>Items search for "<%= params[:word] %>"</h1>
<% else %>
<h1>Customers search for "<%= params[:word] %>"</h1>
<% end %>

<table class="table tabel-hover tabel-inverse">
  <% if @range == '2' %>
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Body</th>
      <th></th>
      <th></th>
      <th colspan='3'></th>
    </tr>
  </thead>
  
  <tbody>
    <% @item.each do |book| %>
    <tr>
      <td>
        <%= link_to(item.customer) do %>
        <%= attachment_image_tag(item.customer, :profile_image, :fill, 60, 60, fallback: "no_image.jpg") %>
        <% end %>
      </td>
      <td>
        <%= link_to item.name, item, class: "item_#{item.id}" %>
      </td>
      <td><%= item.guide %></td>
    </tr>
        <% end %>
  </tbody>
  <% else %>
  
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
      <th></th>
      <th colspan='3'></th>
    </tr>
  </thead>

  <tbody>
    <% @customer.each do |customer| %>  
      <tr>
        <td><%= attachment_image_tag(customer, :profile_image, :fill, 60, 60, fallback: "no_image.jpg") %></td>
        <td><%= customer.name%></td>
        <td><%= link_to "show", customer, class: "customer_#{customer.id}" %></td>
      </tr>
    <% end %>  
  </tbody>
  <% end %>    
</table>