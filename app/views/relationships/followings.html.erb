<h1>Follow Customers</h1>
<% if @customers.exists? %>
  <table table="class" width="100%">
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <td></td>
      <td></td>
      <td></td>
    </tr>
  </thead>
  <tbody>
    <% @customers.each do |user| %>
      <tr>
        <td><%= attachment_image_tag customer, :profile_image, :fill, 60, 60, fallback: "no_image.jpg"%></td>
        <td><%= customer.name %></td>
        <td>フォロー数: <%= customer.followings.count %></td>
        <td>フォロワー数: <%= customer.followers.count %></td>
        <td><%= link_to "Show", current_customer %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<% else %>
  <p>ユーザーはいません</p>
<% end %>