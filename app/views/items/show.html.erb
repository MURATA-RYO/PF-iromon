<h4>タイトル</h4>

<p><%= @item.name %></p>

<h4>説明</h4>

<p><%= @item.guide %></p>

<P>画像</P>
  <%= attachment_image_tag @item, :image, :fill, 200, 200, format: 'jpeg' %>

<%= link_to "編集", edit_item_path(@item.id) %>
<%= link_to "削除", item_path(@item.id), method: :delete, "deta-confirm" => "削除しますか？" %>